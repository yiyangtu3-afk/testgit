# AGENTS.md

# CampusLink 全局 AI 开发指南

CampusLink 是一个基于静态前端和 Spring Boot 后端的校园社交平台
Demo。本文只保留整个仓库通用的开发约定；进入 `frontend/` 或
`backend/` 目录时，优先读取该目录下的 `AGENTS.md`。

---

# 项目目标

项目应持续向模块化、可维护、易扩展演进。新增功能时，优先考虑长期
维护成本，避免为了减少改动而扩大单个文件或单个模块的职责。

---

# 仓库结构

当前主要结构如下：

```text
/
├── AGENTS.md
├── README.md
├── index.html
├── styles.css
├── app.js
├── frontend/
│   ├── AGENTS.md
│   └── js/
├── backend/
│   ├── AGENTS.md
│   └── src/
├── docs/
├── script/
└── tests/
```

根目录保留静态页面入口和通用脚本；前端模块化代码放在 `frontend/`
下，后端 Spring Boot 代码放在 `backend/` 下。

---

# 通用开发原则

开发前先判断：

1. 新代码属于哪个目录或模块。
2. 是否已有代码可以复用。
3. 当前文件是否已经需要拆分。
4. 是否需要先完成小范围重构再开发功能。

始终优先：

* 模块化。
* 可维护性。
* 代码复用。
* 清晰的项目结构。
* 不破坏已有行为。

如果新增代码会明显增加某个文件或类的复杂度，应先拆分模块，再开发
功能。允许并鼓励在开发过程中同步优化目录结构。

---

# 重构原则

出现以下情况，应优先重构：

* 一个文件负责多个业务。
* 同一逻辑重复两次以上。
* 新功能会让现有模块职责变得不清晰。
* 文件长度或复杂度已经影响阅读和测试。

重构必须保持行为兼容，并补充或更新相关测试。

---

# 构建与测试

修改前端时，遵循 `frontend/AGENTS.md` 中的检查命令。修改后端时，
遵循 `backend/AGENTS.md` 中的 Maven 测试要求。

所有修改都应保证：

* 功能正确。
* 不破坏已有行为。
* 保持模块化。
* 无明显重复代码。
* 相关测试通过。

---

# 文档

修改项目结构、API、配置、测试方式或运行方式时，同步更新 `README.md`
或 `docs/` 下的相关文档。

---

# 交接与安全

开始新的开发对话时，先阅读根目录、`frontend/` 和 `backend/` 下的
`AGENTS.md`，再阅读 `docs/new-chat-handoff-2026-07-08.md`、
`docs/phase-two-activity-handoff.md` 和 `docs/resume-project-roadmap.md`。
管理员审核历史的细节见相应的 `docs/admin-*.md` 文档。

除非用户明确授权，不要运行 `git reset --hard`、`git clean`，也不要删除或
清理未跟踪文件。不要擅自重置、重种或清空本地 MySQL 历史数据。

每个可验证的功能阶段完成后，单独提交并推送到 `main`，并同步更新
`README.md` 和相关交接文档。提交前只纳入本阶段的文件，不混入本地环境、
构建产物或无关改动。

---

# 提交要求

提交前确认变更范围清晰，避免混入无关重构、临时文件、构建产物或本地
环境配置。提交说明应描述用户可感知的变化或工程结构变化。
